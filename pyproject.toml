[project]
name = "structural-labels"
version = "0.1.0"
description = "CIFAR-10 SSR baseline for Learning with Structural Labels"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=1.26,<2.0",
    "pillow>=10.0",
    "scipy>=1.14",
    "torch>=2.5",
    "torchvision>=0.20",
    "tqdm>=4.66",
]
